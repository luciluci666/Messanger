INFO = [
    "Here are all available commands:",
    "exit --> disconnects all clients and closes server",
    "kick [client_id] --> disconnects chosen client",
    "close --> closes only server",
    "threads --> shows all threads",
    "clients --> shows list of all clients, [client_id] is index of the client",
    "cmd [client_id] --> opens client's cmd",
    "/sa [message] --> send message for all clients",
]
